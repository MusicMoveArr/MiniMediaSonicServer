ALTER TABLE metadata ADD COLUMN if not exists computed_genre text GENERATED ALWAYS AS (tag_alljsontags->>'Genre') STORED;
CREATE INDEX if not exists idx_metadata_computed_genre ON metadata USING btree (computed_genre);